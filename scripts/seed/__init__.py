"""Standalone seed scripts, runnable as `python -m scripts.seed.<name>`.

Deliberately independent of `app/repositories/`/`app/services/` (broken
mid-restructure during Phase 1) -- write against the ORM model directly.
"""
